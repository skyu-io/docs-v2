import Image from "../../../components/Image";

# Rollback

Since SkyU tracks deployments throughout the project, performing a rollback is as simple as selecting the previous deployment you want to roll back to and then clicking `Re-deploy`.

<Image
  src="/assets/images/releases/releases-rollback.png"
  alt="Rollback deployment screenshot"
  width={1000}
  height={500}
/>
